import 'package:flutter/material.dart';

class Listofearthquakes extends StatelessWidget {
  const Listofearthquakes({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text('list of earthquake'),
      )
    ;
  }
}
